ES6 === ES2015

ES11까지 나온 현재지만 공부안하고 그냥 썼어서 6부터 주르륵 공부하고자한다.

#Scope

let : 블록된 스코프 사용을 위해 사용 / 변경 가능

const : 변하지않는 값을 위해 사용 / 재할당은 불가능하나, 배열과 오브젝트의 값을 변경하는 것은 가능하다.


#전략  
const를 기본으로 사용한다.
그런데 변경이 될 수 있는 변수 는 let을 사용한다.
var는 사용하지 않는다.